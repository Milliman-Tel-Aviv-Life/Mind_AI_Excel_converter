# Preservation

Always work on a copy. Preserve all workbook content unless an authorized operation requires a documented change. Compare semantic structures and OOXML package parts before and after. Classify differences as `AUTHORIZED_CHANGE`, `EXPECTED_RECALCULATION_CHANGE`, `METADATA_CHANGE`, `UNAUTHORIZED_CHANGE`, or `UNVERIFIABLE_CHANGE`. Unauthorized changes block readiness.