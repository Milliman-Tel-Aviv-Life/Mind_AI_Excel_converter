# Reporting

Machine results and human explanations are separate. The application derives status from enums and structured fields, never prose. Every finding and change includes rule IDs and source locators. The LLM never computes the final readiness state.