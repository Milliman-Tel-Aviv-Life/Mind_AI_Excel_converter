# Formula rules

Parse formulas deterministically. Evaluate compatibility against the configured target version. Preserve dependencies, data type, aggregation, volatility, and error semantics. Treat VBA UDFs and unknown custom functions as escalation cases.