# Formatting rules

Preserve styles, number formats, alignment, dimensions, outlines, merged cells, conditional formatting, validation, notes, and hidden states where possible. Warn when round-trip fidelity cannot be demonstrated.