# Workbook rules

Read common Excel formats where possible. Produce only `.xlsx` or `.xlsm`. Preserve `.xlsm` when VBA exists. Multi-workbook coordinated transformation is reserved for a future iteration.