# Naming and flags

Parse recognized grid names and flags without normalizing unrecognized content. Validate uniqueness and required special-grid placement. Preserve case unless a rule explicitly establishes case-insensitive behavior.