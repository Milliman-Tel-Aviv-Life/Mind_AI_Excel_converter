# Transformation policy

Transform only a fresh copy through an immutable, approved change set. Validate preconditions and before hashes. Record every operation. Abort without partial reuse on failure.
