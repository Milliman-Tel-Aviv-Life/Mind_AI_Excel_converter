# Propose formula rewrite

Propose only candidate semantics-preserving rewrites. State facts, assumptions, changed dependencies, error behavior, confidence, required tests, and whether user approval is mandatory.