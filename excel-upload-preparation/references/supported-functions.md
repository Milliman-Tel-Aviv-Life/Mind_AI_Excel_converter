# Supported functions

Populate from reviewed, target-version-scoped documentation. Do not treat this file as timeless. The machine-readable compatibility matrix should be maintained in version overrides.
