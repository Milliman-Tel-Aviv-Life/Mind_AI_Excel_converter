version: 1.0.0
source_document: CompleteMindDocn.docx
note: Generated by tools/mine_kb_docx.py from the Milliman Mind knowledge-base scrape. Do not hand-edit; re-run the script
  against an updated document instead.
kb_pages_total: 431
flags:
- name: Input
  where: title
  kb_term: /Input
  topic: input-manager
  meaning: Grid is an assumptions-only input grid, importable through the Input Manager (CSV named like the grid).
- name: InputSettings
  where: title
  kb_term: /InputSettings
  topic: input-manager-automatch
  unique: true
  meaning: Unique grid mapping /Input grid names (GridName) to CSV filename patterns (FileNamePattern).
- name: Reorder
  where: title
  kb_term: /Reorder
  topic: input-manager-reorder-columns
  meaning: Input columns may arrive in any order; requires /Input, no /NoHeader and unique headers.
- name: NoHeader
  where: title
  kb_term: /NoHeader
  topic: input-manager-reorder-columns
  meaning: Grid has no header row (first row is data).
- name: NoHeaders
  where: title
  kb_term: /NoHeaders
  topic: changelog
  alias_of: NoHeader
  meaning: Alias spelling of /NoHeader seen in release notes.
- name: Export
  where: title
  kb_term: /Export
  topic: export
  meaning: Grid is listed in the Export Manager.
- name: ExportSettings
  where: title
  kb_term: /ExportSettings
  topic: export
  unique: true
  meaning: Unique grid of per-grid export settings (GridName, ExportByInstance, ... AllowScientificFormat).
- name: ExportByInstance
  where: title
  kb_term: /EXPORTBYINSTANCE
  topic: export
  meaning: Export one file per instance (overridden by /ExportSettings).
- name: ExportByLoop
  where: title
  kb_term: /EXPORTBYLOOP
  topic: export
  meaning: Export one file per loop index (overridden by /ExportSettings).
- name: ExportNoInstanceKeys
  where: title
  kb_term: /EXPORTNOINSTANCEKEYS
  topic: export
  meaning: Do not add an InstanceKey column to the export.
- name: ExportNoLoopKeys
  where: title
  kb_term: /EXPORTNOLOOPKEYS
  topic: export
  meaning: Do not add a loop-index column to the export.
- name: InputLink
  where: title
  kb_term: /InputLink
  topic: interlinks
  meaning: Target grid of an interlink (receives data from another project's /OutputLink grid of the same name).
- name: OutputLink
  where: title
  kb_term: /OutputLink
  topic: interlinks
  meaning: Source grid of an interlink.
- name: ProjectSettings
  where: title
  kb_term: /ProjectSettings
  topic: projectsettings-grid
  unique: true
  meaning: 'Unique grid pre-setting project settings: columns Name | Value | Locked | Hidden.'
- name: StepLabels
  where: title
  kb_term: /StepLabels
  topic: step-labels-grid
  unique: true
  meaning: Unique grid defining workflow step labels; must be alone on its own sheet.
- name: Step
  where: title
  kb_term: /Step
  topic: enable-workflow-mode
  meaning: Assigns the grid to a workflow step.
- name: Collapse
  where: title
  kb_term: /Collapse
  topic: enable-workflow-mode
  meaning: Grid is collapsed (3 rows shown) in workflow mode.
- name: Group
  where: title
  kb_term: /Group.(Group
  topic: create-groups-of-grids
  syntax: /Group.(GroupName).x.y
  args: 3
  meaning: 'Grid belongs to a group: shared group name, then vertical (x) and horizontal (y) position starting at 0.'
- name: Parameters
  where: title
  kb_term: /Parameters
  topic: setup-parameters-editor
  meaning: 'Parameters-editor grid: columns Label | Type | PossibleValues | Values...'
- name: Translations
  where: title
  kb_term: /Translations
  topic: translations-of-model
  unique: true
  meaning: 'Translation grid: headers are ISO 639-1 two-letter language codes, first column the design language.'
- name: ReadOnly
  where: title
  kb_term: /ReadOnly
  topic: changelog
  meaning: Grid (or group) is read-only in Mind.
- name: NoFormula
  where: title
  kb_term: /NoFormula
  topic: changelog
  meaning: Formulas of the grid are not shown in Mind (except in debug mode).
- name: Always
  where: title
  kb_term: /Always
  topic: create-dashboards
  meaning: Dashboard displayed at project launch.
- name: AlwaysSave
  where: title
  kb_term: /AlwaysSave
  topic: changelog
  meaning: Grid results are always saved (HPC partial-save option).
- name: Result
  where: title
  kb_term: /Result
  topic: changelog
  meaning: Result grid flag (release notes only; no dedicated article).
- name: HPCInput
  where: title
  kb_term: /HPCInput
  topic: changelog
  meaning: Input grid split across HPC tasks (used with MM_HPCLOOP).
- name: ExtSettings
  where: title
  kb_term: /ExtSettings
  topic: changelog
  unique: true
  meaning: Extension settings grid; the release notes say exactly one is expected when used.
- name: XBRLSettings
  where: title
  kb_term: /XBRLSettings
  topic: changelog
  meaning: XBRL export settings grid.
- name: ValueExclusionSource
  where: title
  kb_term: /valueexclusionsource
  topic: changelog
  meaning: Source grid of the right-click value-exclusion feature.
- name: ValueExclusionDest
  where: title
  kb_term: /ValueExclusionDest
  topic: changelog
  meaning: Destination grid of the value-exclusion feature (read-only).
- name: Resize
  where: title
  kb_term: /Resize
  topic: dynamic-resizing-of-grids
  syntax: /Resize or /Resize.Name
  max_args: 1
  meaning: Grid gets the same number of rows as the other /Resize(Row) grids (of the same named group).
- name: ResizeRow
  where: title
  kb_term: /ResizeRow
  topic: dynamic-resizing-of-grids
  syntax: /ResizeRow or /ResizeRow.Name
  max_args: 1
  meaning: Same as /Resize (rows).
- name: ResizeColumn
  where: title
  kb_term: /ResizeColumn
  topic: dynamic-resizing-of-grids
  syntax: /ResizeColumn or /ResizeColumn.Name
  max_args: 1
  meaning: Grid gets the same number of columns as the other /ResizeColumn grids (of the same named group).
- name: InstanceKeys
  where: title
  kb_term: /InstanceKeys
  topic: introduction-to-instances
  unique: true
  meaning: 'InstanceKey table in the main workbook: headers are the workbook names to duplicate.'
- name: InstanceSplit
  where: title
  kb_term: /InstanceSplit
  topic: manage-grid-data-with-instancesplit
  meaning: Grid data is split by the instance keys in its first column.
- name: InstanceSelect
  where: header
  kb_term: /InstanceSelect
  topic: partial-load-instanceselect
  meaning: Header suffix on the last column of the InstanceKey grid defining instance groups for partial load.
- name: CalculationSteps
  where: title
  kb_term: /CalculationSteps
  topic: iterations-design
  unique: true
  meaning: 'Two-column grid: workbook name | integer calculation step (equal numbers run in parallel).'
- name: iterationinput
  where: title
  kb_term: /iterationinput.(input_name)
  topic: iterations-design
  syntax: /iterationinput.(name)
  args: 1
  meaning: Iteration input grid; paired by name with an /iterationoutput grid.
- name: iterationoutput
  where: title
  kb_term: /iterationoutput.(input_name)
  topic: iterations-design
  syntax: /iterationoutput.(name)
  args: 1
  meaning: Iteration output grid feeding the next iteration's /iterationinput of the same name.
- name: NoKeepResults
  where: title
  kb_term: /NoKeepResults
  topic: iterations-design
  meaning: Keep only the last iteration's results for this grid (saves RAM).
- name: BatchResult
  where: title
  kb_term: /batchresult
  topic: mm_batchindex-function
  meaning: Grid keeps results of every batch (BATCH loop visible) when running batches.
- name: AOC
  where: title
  kb_term: /AOC
  topic: analysis-of-sensitivities-aos-and-analysis-of-change-aoc
  meaning: Input grid with a stretched-values version (Analysis of Change).
- name: AOCOrder
  where: title
  kb_term: /AOCOrder
  topic: analysis-of-sensitivities-aos-and-analysis-of-change-aoc
  unique: true
  meaning: 'Unique grid: Grid Name | AOC Order index | Label.'
- name: AOCGroup
  where: title
  kb_term: /AOCGROUP.(GroupName)
  topic: groups-of-aoc-grids-with-aocgroup
  syntax: /AOCGROUP.(GroupName)
  args: 1
  meaning: Puts an /AOC grid into a named AOC group.
- name: BackupSource
  where: title
  kb_term: /Backupsource.(backup
  topic: grid-backup-feature
  syntax: /BackupSource.(backup name)
  args: 1
  meaning: Source grid of a named backup (exactly one source per backup name).
- name: BackupDest
  where: title
  kb_term: /Backupdest.(backup
  topic: grid-backup-feature
  syntax: /BackupDest.(backup name)
  args: 1
  meaning: Destination grid of a named backup (one or several per backup name).
- name: BackupDestAllDimensions
  where: title
  kb_term: /Backupdestalldimensions.(backup
  topic: grid-backup-feature
  syntax: /BackupDestAllDimensions.(backup name)
  args: 1
  meaning: Destination grid receiving the source grid with all dimensions flattened.
- name: CleanAuditTrailAfterBackup
  where: title
  kb_term: /CleanAuditTrailAfterBackup
  topic: changelog
  meaning: Clears the audit trail after a successful backup.
- name: HideRows
  where: header
  kb_term: /HideRows
  topic: dynamically-hide-rows-columns-grids
  meaning: Header of an extra right-hand column whose TRUE/1 values hide the matching row.
markers:
- marker: '#'
  where: title
  kb_term: preceded by
  meaning: 'A cell whose text starts with ''#'' directly above a grid names it: ''#GridName /Flag1 /Flag2''.'
- marker: '&&Hide'
  where: sheet_name
  kb_term: '&&Hide'
  meaning: Hides an entire sheet in Mind.
- marker: '&&HideColumn'
  where: header
  kb_term: '&&HideColumn'
  meaning: Hides the column whose header contains it.
- marker: '&hide'
  where: header
  kb_term: '&hide'
  meaning: Hides the whole header line of the grid.
- marker: '#NbSimulations'
  where: title
  kb_term: '#NbSimulations'
  meaning: Grid title locking the number of simulations per run.
- marker: °Year0 / °Month0 / °Quarter0 / °Half-year0
  where: field_names_and_formulas
  kb_term: Year0
  meaning: Date indicators shifted from the project date (°Year-1, °Year1, ...).
